; Alguem configurou o servidor (fora do nosso codigo) para injetar um <script> de aviso
; ("App Prototipo... nao encaminhar") na frente de TODA resposta PHP deste dominio --
; inclusive nos endpoints JSON (actions/*.php), quebrando todo fetch().json() do app.
; Isso desliga esse prepend so para esta pasta, sem tocar na configuracao geral do servidor.
auto_prepend_file=
